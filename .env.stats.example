# ========================================
# 统计系统配置 (Stats System Configuration)
# ========================================

# 是否启用统计系统
# Enable statistics collection and reporting
ENABLE_STATS=false

# 角色：CLIENT（交易服务器）或 SERVER（汇总服务器）
# Role: CLIENT (trading bot) or SERVER (aggregation server)
STATS_ROLE=CLIENT

# Bot 名称（用于区分不同实例，仅 CLIENT 角色需要）
# Bot name for identification (CLIENT role only)
BOT_NAME=bot-1

# Server 端接收地址（仅 CLIENT 角色需要）
# Server URL for reporting stats (CLIENT role only)
STATS_SERVER_URL=http://localhost:3000/stats

# Server 端监听端口（仅 SERVER 角色需要）
# Server listening port (SERVER role only)
STATS_SERVER_PORT=3000

# 钉钉机器人 Token（仅 SERVER 角色需要）
# DingTalk bot access token (SERVER role only)
# 获取方式：https://open.dingtalk.com/document/robots/custom-robot-access
DINGTALK_TOKEN=
